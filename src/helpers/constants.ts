
const BASE_URL = 'https://api.spacexdata.com/v3';

const API_ENDPOINTS = {
  LAUNCH: `${BASE_URL}/launches`,
};

const APP_ROUTES = {
  HOME: '/',
};

const CONSTANTS = {
  API_ENDPOINTS,
  APP_ROUTES,
};

export default CONSTANTS;
